# Synth

This is a Next.js project configured with TypeScript, Tailwind CSS, ESLint, the App Router, a `src` directory, and the `@/*` import alias.

## Getting Started

Run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
